using System;

public class RevStr
{
    public static void Main()
    {
        string str = "Hello World!";
        string rev = "";

        for (int i = str.Length - 1; i >= 0; i--)
        {
            rev += str[i];
        }

        Console.WriteLine(rev);
    }
}
