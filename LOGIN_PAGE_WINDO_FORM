private void button1_Click(object sender, EventArgs e)
{
    string user = txtuser.Text;
    string password = txtpass.Text;

    if (user == "BoB" && password == "72083")
        MessageBox.Show("Login Done!");
    else
        MessageBox.Show("Invalid!");
}
