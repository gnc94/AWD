using System;

class Test
{
    public static void Main(string[] args)
    {
        string num1 = "abc";

        try
        {
            int num = int.Parse(num1);   // Invalid format
        }
        catch (FormatException e)
        {
            Console.WriteLine(e.Message);
        }
    }
}
